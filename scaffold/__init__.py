"""Benchmark scaffold: benchmark-agnostic LLM client, metrics schema,
manifest-paired trial runner, and plugin registry.

Concrete benchmarks live under ``benchmarks/<name>/`` and expose a
``BENCHMARK`` object (see scaffold.registry).
"""
